# Lite Handoff: frontend-dashboard

项目定位：这是一个前端展示项目，不是真实运行时。

当前目标：只调整 pricing section 的边界文案。

verified_facts:
- 任务只需要前端层处理。
- 本轮不碰部署和 provider。

confirmed_decisions:
- 不扩大范围。

risk_boundaries:
- 只看两个组件。

tail_request:
请先复述项目状态，再给出下一步。
