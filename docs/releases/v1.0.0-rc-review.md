# v1.0.0 RC Review Package

Date: 2026-06-07

This document is the commit-ready review package for the local BaseBrief v1.0
Delta Handoff candidate.

It is not a push, tag, release, npm publication, provider integration, runtime
integration, plugin, MCP, IDE, schema-v2, or platform claim.

## Changed Surface

The v1.0 candidate adds one local capability:

```text
Reviewable Delta Handoff Compiler
```

Public review entry points:

- adopted plan: `docs/releases/v1.0.0-plan.md`
- release candidate: `docs/releases/v1.0.0.md`
- delta spec: `docs/specs/delta-handoff.md`
- clarity patch: `docs/releases/v1.0.1.md`
- public-safe example: `examples/delta-handoff.md`
- fresh receiver evidence: `docs/dogfooding/delta-handoff-fresh-receiver-v1.0.md`
- baseline-advance evidence: `docs/dogfooding/delta-handoff-baseline-advance-v1.0.md`

Implementation entry points:

- CLI Lite command: `node scripts/basebrief.js delta --repo <target-repo> --output-dir <dir> [--since <commit>] [--advance-baseline] [--json]`
- local implementation: `scripts/basebrief_delta.js`
- release checks: `scripts/run_release_checks.js`
- focused tests: `tests/basebrief.test.js`

## Local-Only Artifacts

The following files are generated local state/output and remain ignored by
`.gitignore`:

```text
.basebrief/delta-baseline.json
.basebrief/out/v1.0-delta/delta-handoff.md
```

They are useful for local dogfooding, but they are not public history and are
not part of the commit-ready tracked package.

## Evidence

Fresh receiver dogfooding passed:

```text
receiver_task_status: completed
repository_state_status: match
handoff_acceptance: pass
blocking_or_repair_notes: none
```

Baseline-advance dogfooding passed:

- first real-repo run wrote `.basebrief/delta-baseline.json`
- second real-repo run became baseline-present
- `basebrief-project-state-v1` remained unchanged
- `.basebrief/` remained ignored and local-only

## Validation

Latest local validation for this candidate:

```text
node scripts/basebrief.js check --input docs/dogfooding/delta-handoff-baseline-advance-v1.0.md --json
git diff --check
node scripts/basebrief.js delta --repo . --output-dir .basebrief/out/v1.0-delta --json
node scripts/basebrief.js check --input .basebrief/out/v1.0-delta --json
npm test
npm run release-check
npm run check
```

Release-check output without provider environment variables must preserve:

```text
provider_probe_status=skipped
```

## Non-Goals

- No provider request.
- No runtime integration.
- No plugin, MCP, IDE, hosted, or cloud-memory work.
- No schema-v2 work.
- No `basebrief-project-state-v2`.
- No `basebrief-sidecar-v2`.
- No Auto Flow.
- No push, tag, release, pull request, npm publish, or global CLI install.
- No raw private output in public docs.

## Commit-Ready Summary

This package is ready for local review as a v1.0 release-candidate change set:
it adds a thin local `delta` command, documents the artifact contract, records
fresh receiver and baseline-advance evidence, and keeps provider/runtime/schema
expansion out of scope.
