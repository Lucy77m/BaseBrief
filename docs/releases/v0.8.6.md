# v0.8.6 Manual Receiver Smoke Result Intake Evidence Candidate

`v0.8.6` records public-safe manual receiver smoke result intake evidence. It
does not add a new CLI command, API, schema, runtime integration, Auto Flow,
tag, release, or npm publish step.

## What Changed

- Accepted public-safe result summaries for the two smallest useful generic
  receiver smoke rows.
- Recorded OpenCode generic as `passed` from a complete public-safe intake
  summary.
- Recorded Claude Code generic as `passed` from a complete public-safe intake
  summary.
- Kept OpenClaw-target rows at `not_run` with `manual_required`.
- Added release-check and unit-test coverage to keep `passed` claims tied to
  complete public-safe intake fields.

## Result

- OpenCode generic receiver smoke: passed.
- Claude Code generic receiver smoke: passed.
- OpenCode openclaw receiver smoke: not_run, manual_required.
- Claude Code openclaw receiver smoke: not_run, manual_required.
- Only the two generic receiver smoke rows are marked `passed`.

## Boundaries

- No raw private output.
- No runtime integration.
- No schema change.
- No Auto Flow.
- No receiver thread creation.
- Wait for user confirmation before continuing from a generated bundle.
- No OpenClaw/Hermes runtime connection.
- No OpenClaw/Hermes profile/config/memory/workspace writes.
- No push, tag, release, or npm publish in this checkpoint.

A `passed` result still requires a public-safe acceptance summary covering
BaseBrief, v0.8.x, current commit, `current_goal`, receiver entry task, at
least two risk boundaries, wait for user confirmation, no auto-advance, no
provider, and no runtime.

Do not write raw receiver output, private absolute paths, secrets, provider
endpoints, model values, token output, or API keys into tracked docs.

`sidecar-build` remains a local Project State consumer for
`basebrief-project-state-v1`. `sidecar-check` remains a read-only structure and
public-safety gate for `basebrief-sidecar-v1` bundles.

`provider_probe_status=skipped`
