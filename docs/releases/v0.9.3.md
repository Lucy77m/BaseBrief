# v0.9.3 Final Closure / Freeze Candidate

`v0.9.3` closes the public `v0.9.x` line without opening a new feature line.
It does not add a new command, schema, provider request, runtime integration,
or Auto Flow behavior. It turns the existing local line into a closed release
candidate: `v0.9.0` defines it, `v0.9.1` explains it, `v0.9.2` gives
public-safe examples, and `v0.9.3` closes and freezes it for release review.

## Closure Goal

The target workflow remains:

```text
receiver-ready.md -> state-init/state-advance -> sidecar-build -> sidecar-check -> new-window-starter.md -> receiver first response
```

This candidate treats the `v0.9.x` line as complete for local release handoff:

- `v0.9.0` = define line
- `v0.9.1` = guide line
- `v0.9.2` = example line
- `v0.9.3` = close/freeze line

## What Changed

- Adds [the v0.9.x Integrated Handoff Closure Matrix](../testing-v0.9.x-test-matrix.md)
  as the aggregate local acceptance record for `v0.9.0` through `v0.9.3`.
- Refreshes README, docs index, testing docs, and roadmap wording so the repo
  no longer describes `v0.9.0` as the current open target.
- Adds release-check and test coverage for the aggregate matrix, the final
  closure doc, and the updated roadmap/current-cycle wording.
- Keeps the public golden path and example kit discoverable without turning
  them into a new command, runtime surface, or `v1.0` claim.

## Boundaries

- No provider request.
- No raw private output.
- No runtime integration.
- No schema change.
- No Auto Flow.
- No plugin or platform work.
- No published npm package or global CLI.
- No v1.0 work.
- No new `v0.9.4+` feature line from this patch.

## Release Gate

The no-provider release gate remains:

```text
provider_probe_status=skipped
```

Required local checks:

```text
node --test tests/basebrief.test.js
node scripts/run_release_checks.js
git diff --check
```
