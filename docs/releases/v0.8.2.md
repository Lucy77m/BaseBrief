# v0.8.2 Sidecar Receiver Acceptance Evidence Candidate

`v0.8.2` records local receiver-acceptance evidence for sidecar bundles. It is
evidence hardening, not a new feature surface.

## What Changed

- Generated ignored private `generic` and `openclaw` sidecar bundles from a
  reviewed Project State source.
- Ran `sidecar-check` against both targets.
- Ran artifact `check --input` against both targets.
- Recorded public-safe evidence without copying raw sidecar output.
- Added the v0.8.x sidecar test matrix.

## Result

- `sidecar-build --target generic`: passed.
- `sidecar-build --target openclaw`: passed.
- `sidecar-check` for `generic`: passed.
- `sidecar-check` for `openclaw`: passed.
- Artifact check for `generic`: passed, 0 errors, 0 warnings.
- Artifact check for `openclaw`: passed, 0 errors, 0 warnings.
- Root `.basebrief/` cleanup: passed.

## Boundaries

- No provider request.
- No raw private output.
- No runtime integration.
- No schema change.
- No Auto Flow.
- No receiver thread creation.
- Wait for user confirmation before continuing from a generated bundle.
- OpenClaw target remains safety wording only.
- No OpenClaw/Hermes runtime connection.
- No OpenClaw/Hermes profile/config/memory/workspace writes.
- No push, tag, release, or npm publish in this checkpoint.

`sidecar-check` remains a structure and public-safety acceptance gate. It is
not a provider smoke test and not a receiver matrix across tools or models.

`basebrief-project-state-v1` and `basebrief-sidecar-v1` are unchanged.

`provider_probe_status=skipped`
