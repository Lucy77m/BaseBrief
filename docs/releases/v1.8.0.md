# v1.8.0 Delta Receiver Lint Dogfooding Evidence Local Closeout

Date: 2026-06-07

This document records the local closeout state for the BaseBrief v1.8 Delta
Receiver Lint Dogfooding Evidence line.

It is not a push, tag, release, package publication, provider integration,
runtime integration, plugin, MCP, IDE, hosted service, platform claim, Auto
Flow behavior, global CLI install, npm publication, command output format
change, JSON schema claim, schema-v2 claim, or checker rule change.

## Summary

BaseBrief v1.8 closes one narrow local-first evidence slice:

```text
Delta Receiver Lint Dogfooding Evidence
```

The line records public-safe dogfooding over the v1.6 fixtures, v1.7 repair
examples, and existing public receiver examples. It does not change checker
rules, CLI commands, schemas, runtime behavior, provider behavior, or command
output.

## Closeout Contents

This local closeout includes:

- planning baseline: `docs/releases/v1.8.0-plan.md`
- dogfooding evidence:
  `docs/dogfooding/delta-receiver-lint-dogfooding-v1.8.md`
- validation of the v1.6 intentional fixture outcomes
- validation of the v1.7 fixed examples
- validation of existing receiver examples such as
  `examples/receiver/language-routing/receiver-report.md`,
  `examples/receiver/difference-found/receiver-check-result.json`, and
  `examples/receiver/usage-pack/starter-report-outline.md`
- documentation updates in `docs/checks.md`, `docs/receiver-usage-pack.md`,
  `docs/testing.md`, `docs/index.md`, and
  `docs/roadmap/basebrief-long-term-baseline.md`
- release-check and independent test coverage for the evidence record

## Evidence Contract

The dogfooding evidence keeps the receiver lint line public-safe:

- it records command shapes and checker summaries, not raw private output
- it treats intentional failing fixtures as expected receiver lint findings
- it treats fixed examples as clean references that must pass with zero
  findings
- it keeps `difference_found` as a completed verification result, not an agent
  failure
- it keeps historical `commits_in_range` drift non-blocking when refreshed
  branch, HEAD, and worktree facts still match live repository state

## Validation

The local validation gate for this closeout is:

```text
node scripts/basebrief.js check --input examples/receiver/lint/repair/fixed-delta-receiver-report.md --json
node scripts/basebrief.js check --input examples/receiver/lint/repair/fixed-starter-report.md --json
node scripts/basebrief.js check --input examples/receiver/lint/repair/fixed-result.json --json
node scripts/basebrief.js check --input docs/dogfooding/delta-receiver-lint-dogfooding-v1.8.md --json
node scripts/run_release_checks.js
npm run check
git diff --check
```

When provider environment variables are absent, release checks must keep:

```text
provider_probe_status=skipped
```

## Non-Goals

- No provider request.
- No runtime integration.
- No plugin, MCP, IDE, hosted, or cloud-memory work.
- No schema-v2 work.
- No `basebrief-project-state-v2`.
- No `basebrief-sidecar-v2`.
- No new CLI command.
- No new npm script.
- No machine-readable JSON schema.
- No command output format change.
- No Auto Flow.
- No checker rule change.
- No push, tag, release, pull request, npm publish, or global CLI install.
- No raw private output in public docs.

## Status

The v1.8 Delta Receiver Lint Dogfooding Evidence line is locally closed after
the public-safe evidence record, docs discoverability, release-check coverage,
and independent tests were aligned around the v1.6 fixture pack and v1.7 repair
pack.

Publication, tags, pushes, pull requests, provider work, runtime work, plugin
work, MCP work, IDE work, hosted work, command output format work, report
schema work, schema-v2 work, and checker rule changes remain out of scope.
