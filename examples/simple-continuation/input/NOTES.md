# Notes

Useful context from the previous AI session:

- The user prefers small UI polish over a rewrite.
- The project should stay local and simple.
- The next AI should recheck the live repo before editing.

Open questions:

- Which UI file owns the empty-state copy?
- Does the README already mention the dev command?
